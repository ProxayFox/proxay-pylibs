# python_template

Python Repository Template

## Development container

The devcontainer is built from `.devcontainer/Dockerfile`, which uses the base image `mcr.microsoft.com/devcontainers/python:3-3.14-trixie`.
