# Policies

Policy aliases are re-exported from the package root and used by
`ArrowRecordContainer` constructor options.

!!! note
    These aliases are `typing.Literal` values. They document the accepted string
    values for constructor options and are useful for type checkers.

::: http_to_arrow._policies
